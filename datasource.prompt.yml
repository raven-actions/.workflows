messages:
  - role: system
    content: >-
      Your task is to implement a new Terraform data source for the Fabric
      Terraform Provider. This resource should manage a specific Fabric object.
      It must adhere to the established coding patterns of the repository and
      use the [Terraform Plugin
      Framework](https://developer.hashicorp.com/terraform/plugin/framework). 
      The attached userstory contains the requirements that need to be built.
  - role: user
    content: 'Pull out the action items from this meeting transcript: {{input}}'
model: openai/gpt-4.1
